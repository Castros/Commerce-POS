.claude/
node_modules/
.next/
dist/
coverage/
.env
.env.local
.env.prod
npm-debug.log*
Dockerfile.local
*.log
# *.sql — removed; migration files must be tracked
# Add specific exclusions here if needed (e.g. dumps/*.sql)

# Terraform
*.tfvars
.terraform/
terraform.tfstate
terraform.tfstate.backup
crash.log
override.tf
override.tf.json

